sort 文本排序
    - "-u" 移除重复行
    - "-n" 数字大小排序（从小到大排序）
    - "-r" 从大到小排序
    - "-t" 分隔符
    -  "-k" 按照目标列排序
    
uniq 文本除掉连续重复行
    - "-c" 统计重复行
    - "-d" 显示一次重复行
    - "-D" 显示所有重复行
    - "-u" 不显示重复行
    - "-w" 选择字符去除重复行
    - "-f" 跳过目标空格去除重复行
    
tr 替换删除字符
    - 小写字母换大写字母：tr a-z或[:lower:] A-Z或[:upper:]
    - 小括号换中括号：tr '()' '[]'
    - [:space:] 空格
    
    - "-d" 删除目标
    - "-c" 非
    
grep 显示含有目标字符串的行
    - "-i" 忽略大小写
    - "-r/-R" 递归
    - "-w" 显示只包含目标的行
    - "-c" 匹配次数
    - "-n" 多显示匹配的行号
    - "-v" 显示没有匹配上的行
    - "--color" 以彩色输出
    
diff 比较两个文件
    - "1,3c1,3" 1到3行有区别
    - "4a5,6" 在4行后面多了5,6两行
    - "7,11c9,13" 内容有差别
    
    - "-w" 忽略空格
    - "-y" 比较结果
        - ">" 第二个文件多出的行
        - "<" 第一行文件多出的行
    - "-W" 列宽
    - "-c" 以上下对比的格式输出比较结果
        - "!" 有差别的行
        "+" 第二个文件比第一个放出的行
        
paster 将文件合并
    - "-d" 使用符号分割
    - "-s" 将每一个文件合并成一行

dd 备份和拷贝文件
    if 标准输入 of标准输出
    格式： dd if=输出路径 of=输入路径
    
gzip bzip2压缩 归档（gz bz2）
    gzip
    - "-c" 自定义文件压缩名字 保留原文件
    - "-d" 解压文件
    - "-r" 递归压缩文件 不保留原文件
    - "-数字" 压缩级别 -1速度最快 压缩率最低 -9最慢压缩率最好
    bzip 压缩后缀为.bz2 比gzip有更好的压缩率 速度更慢
    - "-k" 保留原文件压缩
    - "-d" 解压缩
    - "-f" 解压缩
    一般默认级别-9
gunzip bunzip2解压缩
    - "-c" 自定义解压文件名字 保留原文件
    
    buzip2
    - "-k" 保留原文件解压
    
tar 打包和解包
    - "-c" 建立新的归档
    - "-x" 对于归档进行解压
    - "-t" 列出tar包文件
    - "-v" 列出被处理的文件
    - "-r" 将新文件添加到已经存在tar包里
    - "-f" 归档文件名所使用的参数
    - "-z" 可以用gzip进行压缩/解压
    - "-j" 可以用bzip进行压缩/解压
    - "-W" 核实tar包的内容
    - "-d" 比较压缩tar包内容