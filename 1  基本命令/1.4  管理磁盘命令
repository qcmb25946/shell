mount umount挂载与卸载存储
    - "-t" 显示固定格式挂载的信息
    - "-o ro" 只能读取挂载
    - "-o loop" 挂在文件

    umount 卸载挂载
    
df 查看硬盘存储数据
    - "-a" 查看文件系统所有数据
    - "-h" 以g为单位查看
    - "-T" 查看文件系统格式
    - "-t" 指定文件数据
    - "-x" 除指定以外文件信息
    - "-m" 以兆为单位查看